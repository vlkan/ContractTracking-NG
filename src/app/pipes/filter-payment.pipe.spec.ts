import { FilterPaymentPipe } from './filter-payment.pipe';

describe('FilterPaymentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPaymentPipe();
    expect(pipe).toBeTruthy();
  });
});
